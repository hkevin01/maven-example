# Environment Configuration (Profile-specific)
log.level=${log.level}
environment=${maven.profiles.active}
debug.enabled=false

# Runtime Settings
max.memory=512m
thread.pool.size=10
request.timeout=5000

# Monitoring
metrics.enabled=true
health.check.interval=60
performance.tracking=enabled
