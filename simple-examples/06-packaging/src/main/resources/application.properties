# Application Configuration
app.name=Maven Packaging Examples
app.version=1.0.0
app.description=Demonstrates different Maven packaging strategies

# Build Information
build.profile=${maven.profiles.active}
packaging.type=${packaging.type}

# Runtime Configuration
log.level=INFO
debug.mode=false

# Feature Flags
features.web.enabled=true
features.json.processing=true
features.manifest.info=true
