# Build Information Properties
# This file is manually created to demonstrate build metadata

build.system=Maven
build.tool.version=3.6+
build.java.version=${java.version}
build.timestamp=${maven.build.timestamp}
build.user=${user.name}
build.os=${os.name}

# Project Information
project.name=${project.name}
project.group=${project.groupId}
project.artifact=${project.artifactId}
project.version=${project.version}
project.packaging=${project.packaging}

# Compiler Information
compiler.source=${maven.compiler.source}
compiler.target=${maven.compiler.target}
encoding=${project.build.sourceEncoding}
